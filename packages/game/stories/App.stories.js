import React from "react";
import { App } from "../src/App";

export default { title: "App" };

export const app = () => {
  return <App />;
};
app.story = {
  name: "App",
};

app.story = {
  name: "Game Widget",
};
