import React, { useState, useEffect } from "react";
import IM from "../src/IMessage";

export default { title: "IMessage" };

export const app = () => {
  return <IM data={{}} />;
};
app.story = {
  name: "IMessage",
};
